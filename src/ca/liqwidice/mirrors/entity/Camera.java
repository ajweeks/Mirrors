package ca.liqwidice.mirrors.entity;

public class Camera {
	//LATER implement camera later if needed
	//	private int xo, yo;
	//	private Rectangle bounds;
	//
	//	public Camera(Rectangle bounds) {
	//		this.bounds = bounds;
	//		xo = 0;
	//		yo = 0;
	//	}
	//
	//	public void update(double delta) {
	//
	//		clamp();
	//	}
	//
	//	public void clamp() {
	//		if (xo < 0) xo = 0;
	//		if (xo > bounds.x) xo = bounds.x;
	//		if (yo < 0) yo = 0;
	//		if (yo > bounds.y) yo = bounds.y;
	//	}
}
